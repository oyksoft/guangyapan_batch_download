# 光鸭云盘 - 获取直链

一个轻量级的油猴（Tampermonkey）脚本，为光鸭云盘网页版提供批量获取文件直链的功能。

## 功能特点

- **批量获取直链** - 一次选择多个文件，一键获取所有直链
- **智能识别选中文件** - 通过 React 组件状态获取选中文件，不受虚拟滚动影响
- **Aria2 一键下载** - 集成 Aria2 RPC，支持直接发送到下载队列，显示文件大小和总体积
- **并发请求** - 支持并发获取直链，提高效率
- **失败重试** - 网络波动时自动重试（最多 3 次，指数退避）
- **取消功能** - 获取过程中可随时取消
- **复制方便** - 支持一键复制全部链接，或只复制选中的链接
- **路由适配** - 在特定路由（如 `#/transfer/cloud`）下不显示按钮

## 安装使用

### 方式一：直接安装（推荐）

访问 GreasyFork 页面直接安装：
https://greasyfork.org/zh-CN/scripts/575452-%E5%85%89%E9%B8%AD%E4%BA%91%E7%9B%98-%E8%8E%B7%E5%8F%96%E7%9B%B4%E9%93%BE

### 方式二：手动安装

1. 安装油猴扩展：[Tampermonkey](https://www.tampermonkey.net/)（Chrome、Firefox、Edge 等主流浏览器均支持）
2. 点击脚本文件 `guangyapan_batch_download.user.js`，复制全部内容
3. 打开油猴扩展，点击左侧「+」号新建脚本
4. 粘贴代码并保存

## 使用方法

1. 打开光鸭云盘网页版：https://www.guangyapan.com
2. 进入文件所在目录，选中需要获取直链的文件
3. 点击「获取直链」按钮
4. 在弹窗中查看直链，支持复制全部或部分链接

## 工作原理

脚本通过遍历 React Fiber 树，读取 `FileList` 组件的 `props.dataSource` 和 `props.selectedItems` 来获取：

- 完整文件列表（用于建立 fileId 到文件名的映射）
- 当前选中的文件 ID 集合

这种方法可以正确处理虚拟滚动场景，即使文件列表只渲染了屏幕上可见的部分，也能获取所有选中文件的信息。

## 技术细节

| 项目 | 说明 |
|------|------|
| 文件 ID 格式 | 19 位数字字符串（如 `1895377741382598728`） |
| 文件类型判断 | `resType: 1` = 文件，`resType: 2` = 文件夹 |
| API 地址 | `https://api.guangyapan.com/nd.bizuserres.s/v1/get_res_download_url` |
| 并发数 | 3 |
| 请求超时 | 10 秒 |
| 重试策略 | 指数退避（500ms, 1s, 2s） |

## 更新日志

### v1.6 - Firefox 兼容性与 UI 优化
- 🐛 **修复 Firefox 无法获取链接** - 解决 Firefox 浏览器下无法获取文件直链的问题
- 💡 **授权提示** - 非 localhost/127.0.0.1 域名发送 Aria2 时提示选择"总是允许"，一劳永逸
- ✅ **成功弹窗** - 发送完成后显示绿色成功弹窗，带动画打勾效果，3 秒自动关闭
- ❌ **错误弹窗** - 发送失败时显示红色错误弹窗，列出失败文件名及原因，点击"我知道了"关闭
- 🛑 **中断机制** - Aria2 发送遇错即停，不再继续发送后续文件
- 🎨 **按钮优化** - 按钮文字图标垂直居中，样式更紧凑
- 🔒 **弹窗交互** - 获取直链弹窗点击外部不再自动关闭，需手动点击关闭按钮

### v1.5 - Aria2 下载支持
- ✨ **新增 Aria2 一键下载** - 集成 Aria2 RPC，可直接发送文件到下载队列
- 新增文件大小列 - 从 API 原始数据直接获取
- 确认弹窗 - 下载前显示文件数量和总体积
- 配置弹窗 - Shadow DOM 隔离 React 事件，配置项可正常输入和复制
- UI 优化 - 操作按钮带图标，更紧凑美观

### v1.4
- 优化进度条样式，文字与进度条同行显示，百分比更直观
- 进度条采用渐变紫色效果，带闪光动画
- 表头背景颜色减淡，更加清新
- 优化批次间隔，批量获取速度更快
- 清理未使用的函数和变量，代码更精简

### v1.3
- 界面全新升级，按钮采用渐变豪华效果
- 表头样式优化，渐变色彩配浅色系
- 复制文件名、复制直链按钮改为渐变紫色/粉色
- 修复 SPA 路由切换问题，按钮现在在 `/home/` 开头的路由下正确显示
- 修复取消获取时的提示信息（正确显示"用户取消获取"）
- 修复重新打开弹窗时按钮状态不正确的 bug
- 优化表头和列表列宽，各列宽度协调统一
- 按钮定位调整为"上传"按钮之后

### v1.2
- 支持从 React 组件状态获取选中文件，解决虚拟滚动问题
- 新增请求重试机制
- 新增取消获取功能
- 优化文件夹过滤逻辑

## 注意事项

- 仅支持 `https://www.guangyapan.com/*` 域名
- 需要保持登录状态
- 部分功能可能因网站更新而失效

## License

MIT License
